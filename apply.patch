*** Begin Patch
*** Update File: apps/web/src/components/shell.tsx
--- apps/web/src/components/shell.tsx
+++ apps/web/src/components/shell.tsx
@@ -73,7 +73,9 @@
             return (
               <Link
                 key={item.href}
-                href={item.href as never}
-                className={lock rounded-xl px-4 py-3 text-sm font-medium transition }
+                href={item.href as never} 
+                className={lock rounded-xl px-4 py-3 text-sm font-medium transition }
               >
                 {item.label}
               </Link>
@@ -107,7 +109,9 @@
           {menu.map((item) => (
             <Link
               key={item.href}
-              href={item.href as never}
-              className={whitespace-nowrap rounded-lg px-3 py-2 text-sm }
+              href={item.href as never} 
+              className={whitespace-nowrap rounded-lg px-3 py-2 text-sm }
             >
               {item.label}
             </Link>
@@ -140,7 +144,7 @@
   return (
     <div className="card p-5">
       <p className="text-xs uppercase tracking-wide text-slate-500">{label}</p>
-      <p className={mt-2 text-2xl font-bold }>{value}</p>
+      <p className={mt-2 text-2xl font-bold }>{value}</p>
     </div>
   );
 }
*** End Patch
